# 📦 Requirements for Data Preparation

## Python Dependencies

```bash
# คำสั่งติดตั้ง dependencies ทั้งหมด
pip install opencv-python pillow boto3 tqdm pyyaml numpy

# หรือติดตั้งจากไฟล์ requirements
pip install -r requirements.txt
```

## requirements.txt

```
# Image processing
opencv-python>=4.5.0
Pillow>=8.0.0
numpy>=1.20.0

# Progress bars and utilities
tqdm>=4.60.0

# AWS integration
boto3>=1.20.0
botocore>=1.23.0

# Data formats
PyYAML>=5.4.0

# Optional: for better image handling
imageio>=2.9.0
scikit-image>=0.18.0
```

## System Requirements

- **Python**: 3.8+ (แนะนำ 3.9)
- **OS**: Windows, macOS, Linux
- **Memory**: อย่างน้อย 4GB RAM
- **Storage**: อย่างน้อย 10GB ว่าง

## AWS Requirements

### AWS CLI
```bash
# ติดตั้ง AWS CLI
pip install awscli

# ตั้งค่า credentials
aws configure
```

### AWS Permissions
S3 IAM Policy ที่จำเป็น:
```json
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "s3:GetObject",
                "s3:PutObject",
                "s3:DeleteObject",
                "s3:ListBucket"
            ],
            "Resource": [
                "arn:aws:s3:::your-bucket-name",
                "arn:aws:s3:::your-bucket-name/*"
            ]
        }
    ]
}
```

## Quick Setup

```bash
# 1. Clone หรือดาวน์โหลดโปรเจค
cd paddle_ocr_sagemaker/data_preparation

# 2. สร้าง virtual environment (แนะนำ)
python -m venv venv
source venv/bin/activate  # Linux/macOS
# หรือ venv\Scripts\activate  # Windows

# 3. ติดตั้ง dependencies
pip install -r scripts/requirements.txt

# 4. ตั้งค่า AWS
aws configure

# 5. เตรียมข้อมูล
# วางไฟล์ในโฟลเดอร์ input/
# รัน: python scripts/convert_data.py
```

## Troubleshooting

### OpenCV ติดตั้งไม่ได้
```bash
# ลองใช้ version อื่น
pip install opencv-python-headless

# หรือใช้ conda
conda install opencv
```

### PIL/Pillow ปัญหา
```bash
# ถ้ามี PIL เก่าอยู่
pip uninstall PIL
pip install Pillow
```

### AWS Credentials ปัญหา
```bash
# ตรวจสอบ credentials
aws sts get-caller-identity

# ตั้งค่าใหม่
aws configure --profile default
```
